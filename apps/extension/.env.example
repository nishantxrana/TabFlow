# TabFlow Extension Environment Variables
# Copy this file to .env.development (for local dev) or .env.production (for builds)

# Cloud API URL
# Development: Use localhost Azure Functions
# Production: Use deployed Azure Functions URL
VITE_CLOUD_API_URL=http://localhost:7071/api

# Encryption Key for Cloud Sync
# IMPORTANT: Use a strong, unique key in production!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
VITE_ENCRYPTION_KEY=your-strong-encryption-key-here
